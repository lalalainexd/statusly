== Statusly

This is a very basic microblogger. Users can create an account and post a status. Users can also request to follow other users. If their request is accepted, then the user being followed is added to the feed which is live updated.
